pipeline {
    agent any
    tools {
        maven 'Maven_3' // Make sure this matches the Maven name in Jenkins
    }
    stages {
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }
    }
}